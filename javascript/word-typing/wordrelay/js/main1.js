const word = document.querySelector(".word");
const wordList = document.querySelector(".word-list");

const firtstWords = ["강아지", "소나기", "기상청"];
const firstWord = firstWords[Math.floor(Math.random) * 3];
